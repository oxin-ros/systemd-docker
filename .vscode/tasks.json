{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build systemd-docker",
            "type": "shell",
            "command": "go build -mod vendor -o Build/systemd-docker"
        }
    ]
}